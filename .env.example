# --- Application Configuration ---
# Set to "true" or "1" for detailed logging and debugging features.
DEBUG=True

# Defines the execution backend. Use 'local' for single-machine mode (requires manual worker start)
# or 'distributed' for Redis-based queueing (recommended for Docker).
EXECUTION_BACKEND=distributed

# --- Service Configuration ---
# The URL for the Redis instance, used for job queueing, state, and caching.
REDIS_URL=redis://redis:6379/0

# --- Security ---
# The secret API key required for accessing protected endpoints (e.g., POST /jobs).
# Clients must provide this key in the 'X-API-Key' header.
API_KEY=your-secret-api-key-here

# --- Worker Configuration ---
# Specifies the model ID that a worker instance should load and process jobs for.
# This allows for scaling workers dedicated to specific models.
# Example: WORKER_MODEL_ID=faster_large-v3_fp16
WORKER_MODEL_ID=distil_large_v3_ptbr

# --- Logging Configuration ---
# The minimum level for logs (e.g., DEBUG, INFO, WARNING, ERROR).
LOG_LEVEL=INFO
